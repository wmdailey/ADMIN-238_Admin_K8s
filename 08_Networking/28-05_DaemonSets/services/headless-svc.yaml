# Exercise 28-05 v3.2.0
#
---
apiVersion: v1
kind: Service
metadata:
  name: headless-svc
spec:
  clusterIP: None
  selector:
    name: web-check-pod
  ports:
    - protocol: TCP
      name: "http"
      port: 80
      targetPort: 80
