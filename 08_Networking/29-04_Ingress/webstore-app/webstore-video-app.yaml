# Exercise: 29-04 v3.2.0
#
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: webstore-video-cm
  namespace: webstore-ns
data:
  order.html: |
    "/order.html" on webstore-video-svc
  video.html: |
    "/video.html" on webstore-video-svc
  index.html: |
    "/" on webstore-video-svc
  404.html: |
    webstore-video 404 page
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: webstore-video-nginx-cm
  namespace: webstore-ns
data:
  nginx.conf: |
    user  nginx;
    worker_processes  1;
    error_log  /var/log/nginx/error.log warn;
    pid        /var/run/nginx.pid;
    events {
        worker_connections  1024;
    }

    http {
        sendfile        on;
        server {
          listen       80;
          server_name  localhost;

          location / {
              root   /usr/share/nginx/html;
              index  index.html index.htm;
          }

          error_page 404 /404.html;
          error_page   500 502 503 504  /50x.html;
          location = /50x.html {
              root   /usr/share/nginx/html;
          }
        }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: webstore-video-deploy
  namespace: webstore-ns
  labels:
    app: webstore-video
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webstore-video-pod
  template:
    metadata:
      labels:
        app: webstore-video-pod
    spec:
      containers:
      - name: nginx
        image: nginx:1.14.2
        ports:
        - containerPort: 80
        volumeMounts:
        - name: html
          mountPath: "/usr/share/nginx/html/"
        - name: config
          mountPath: "/etc/nginx/"
        resources: {}
      volumes:
      - name: html
        configMap:
          name: webstore-video-cm
      - name: config
        configMap:
          name: webstore-video-nginx-cm
---
apiVersion: v1
kind: Service
metadata:
  name: webstore-video-svc
  namespace: webstore-ns
spec:
  selector:
    app: webstore-video-pod
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
