# Exercise 21-04 v3.3.0
#
---
apiVersion: v1
kind: Pod
metadata:
  name: db02-pod 
spec:
  containers:
  - name: db-app 
    image: redis
    resources:
      requests:
        cpu: "400m"
        memory: "700Mi"
      limits:
        cpu: "800m"
        memory: "1.5Gi"
